# Server (porta via env: SERVER_PORT)
server.port=${SERVER_PORT:8080}

# Application
spring.application.name=axono-backend

# CORS: origens permitidas (env: APP_CORS_ALLOWED_ORIGINS, separado por vírgula)
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,https://axono.vercel.app}

# Database - use variáveis de ambiente em produção
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-blue-moon-acpbncxi-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_wU79FyAzHPIE}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update

# JWT
jwt.secret=axono-internato-jwt-secret-key-min-256-bits-for-hs256
jwt.expiration-ms=86400000
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
